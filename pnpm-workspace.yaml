packages:
  - "packages/*"
  - "apps/*"
  - "server"

# esbuild links a platform binary in its install script; vitest needs it.
allowBuilds:
  better-sqlite3: set this to true or false
  esbuild: true
